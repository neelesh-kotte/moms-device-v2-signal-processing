abledWhy').textContent='Ready to record.'}}
$('#beginRecording').onclick=()=>{secondsLeft=60;setRecordStep(2);updateTimer();timerHandle=setInterval(()=>{secondsLeft--;updateTimer();if(secondsLeft<=0)finishRecording()},1000)};function updateTimer(){$('#timer').textContent=`${String(Math.floor(secondsLeft/60)).padStart(2,'0')}:${String(secondsLeft%60).padStart(2,'0')}`}
function finishRecording(){clearInterval(timerHandle);timerHandle=null;setRecordStep(3)}$('#finishEarly').onclick=finishRecording;$('#cancelRecording').onclick=()=>{clearInterval(timerHandle);timerHandle=null;if(confirm('Cancel this recording? The unfinished recording will be discarded.'))setRecordStep(0)};$('#retryRecording').onclick=()=>setRecordStep(0);
$('#hunger').oninput=e=>$('#hungerVal').textContent=Number(e.target.value).toFixed(1)+' / 10';
$('#saveCheckin').onclick=async()=>{if(!profile)return;const minutes=$('#mealMinutes').value.trim(),note=$('#contextNote').value.trim();const payload={owner_id:user.id,profile_id:profile.id,hunger_rating:Number($('#hunger').value),minutes_since_eating:minutes?Math.max(0,Math.round(Number(minutes))):null,optional_context:note?{note}:{} };const r=await sb.from('mom_checkins').insert(payload);if(r.error)return $('#checkinMsg').textContent=r.error.message;$('#checkinMsg').textContent='Check-in saved ✓ Added to this profile’s personal history.';setTimeout(async()=>{await loadData();jumpTab('home');setRecordStep(0)},800)};$('#skipCheckin').onclick=()=>{setRecordStep(0);jumpTab('home')};
function renderPrefs(){$$('#prefChips button').forEach(b=>b.classList.toggle('on',selectedPrefs.has(b.dataset.pref)));if(!selectedPrefs.size){$('#foodIdeas').className='empty';$('#foodIdeas').textContent='Save preferences to see simple matches.';return}const ideas=[];if(selectedPrefs.has('Warm'))ideas.push('Warm bowl or soup');if(selectedPrefs.has('Fresh'))ideas.push('Fruit, salad, or fresh wrap');if(selectedPrefs.has('Savory'))ideas.push('Savory snack or simple meal');if(selectedPrefs.has('Sweet'))ideas.push('Fruit, yogurt, or another saved sweet preference');if(selectedPrefs.has('Crunchy'))ideas.push('Crunchy snack option');if(selectedPrefs.has('Filling'))ideas.push('More filling meal option');if(selectedPrefs.has('No preference'))ideas.push('Use practical constraints only');$('#foodIdeas').className='';$('#foodIdeas').innerHTML=(ideas.length?ideas:['Preference match']).slice(0,3).map(x=>`<div class="idea"><strong>${safe(x)}</strong><p class="small">Matches your saved preference categories.</p></div>`).join('')}
$$('#prefChips button').forEach(b=>b.onclick=()=>{if(b.dataset.pref==='No preference')selectedPrefs=new Set(['No preference']);else{selectedPrefs.delete('No preference');selectedPrefs.has(b.dataset.pref)?selectedPrefs.delete(b.dataset.pref):selectedPrefs.add(b.dataset.pref)}renderPrefs()});
$('#savePrefs').onclick=async()=>{if(!profile)return $('#prefMsg').textContent='Choose a profile first.';const payload={owner_id:user.id,profile_id:profile.id,categories:[...selectedPrefs],constraints:{notes:$('#constraints').value.trim()}};let r=prefs?await sb.from('mom_preferences').update(payload).eq('id',prefs.id):await sb.from('mom_preferences').insert(payload);if(r.error)return $('#prefMsg').textContent=r.error.message;$('#prefMsg').textContent='Preferences saved.';await loadData()};$('#clearPrefs').onclick=()=>{selectedPrefs.clear();$('#constraints').value='';renderPrefs()};
function renderDevice(){const d=devices[0],online=isOnline();$('#deviceDot').className='dot '+(online?'good':'');$('#deviceStatus').textContent=online?'Device connected':'Device offline';$('#deviceHeading').textContent=online?'MOM device connected':'MOM device offline';$('#deviceExplain').textContent=online?'A paired device checked in recently and can upload to the cloud.':'The cloud dashboard is available, but no paired device has checked in recently.';$('#espState').textContent=online?'Connected':'Offline';$('#wifiState').textContent=online?'Connected':'Unknown';$('#lastSync').textContent=d?.last_seen_at?fmt(d.last_seen_at):'Never';$('#firmware').textContent=d?.firmware_version||'Unknown'}
async function sha256(t){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(t));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}function makeToken(){const a=new Uint8Array(32);crypto.getRandomValues(a);return 'mom_'+[...a].map(x=>x.toString(16).padStart(2,'0')).join('')}
$('#pair').onclick=async()=>{if(!profile)return $('#pairOut').innerHTML='<div class="notice">Choose a profile first.</div>';let d=devices[0];if(!d){const r=await sb.from('mom_devices').insert({owner_id:user.id,profile_id:profile.id,display_name:'MOM Device',hardware:'ESP32 + MAX4466'}).select().single();if(r.error)return $('#pairOut').textContent=r.error.message;d=r.data}const t=makeToken(),h=await sha256(t),r=await sb.from('mom_device_keys').insert({owner_id:user.id,device_id:d.id,token_hash:h});if(r.error)return $('#pairOut').textContent=r.error.message;$('#pairOut').innerHTML=`<div class="notice good"><strong>Device credential created.</strong><p class="small">Copy this once into the ESP32 firmware/config. MOM stores only the hash.</p><div class="code">${safe(t)}</div><p class="small">Cloud endpoint</p><div class="code">${SUPABASE_URL}/functions/v1/mom-device-ingest</div></div>`;await loadData()};
$('#exportData').onclick=()=>{if(!profile)return;downloadJson(`mom-${profile.display_name.replace(/\W+/g,'-').toLowerCase()}-export.json`,{profile,sessions,checkins,preferences:prefs,devices:devices.map(({id,display_name,hardware,firmware_version,last_seen_at})=>({id,display_name,hardware,firmware_version,last_seen_at}))})};
$('#deleteProfile').onclick=async()=>{if(!profile)return;if(!confirm(`Delete profile “${profile.display_name}” and its linked data? This cannot be undone.`))return;const r=await sb.from('mom_profiles').delete().eq('id',profile.id);if(r.error)return alert(r.error.message);profile=null;await loadProfiles();jumpTab('home')};
function downloadJson(name,obj){const blob=new Blob([JSON.stringify(obj,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function topNotice(m){$('#homeNotice').innerHTML='<div class="notice">'+safe(m)+'</div>'}
(async()=>{const {data:{session}}=await sb.auth.getSession();if(session?.user)user=session.user;showView('publicView')})();
</script>
</body>
</html>
