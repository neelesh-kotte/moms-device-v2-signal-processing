nerHTML='<div class="notice good">Device ready. MOM is not recording.</div>'}$('#progressText').textContent=usable<4?'Getting started. MOM needs more clean sessions before evaluating personal patterns.':usable<8?'Building history. MOM is collecting more usable examples.':'Enough history exists to evaluate whether stable personal patterns appear.';const recentItems=[...sessions.map(x=>({t:x.started_at,label:`Recording · ${x.quality_label||'quality pending'}`})),...checkins.map(x=>({t:x.created_at,label:`Check-in · ${x.hunger_rating==null?'no rating':Number(x.hunger_rating).toFixed(1)+'/10'}`}))].sort((a,b)=>new Date(b.t)-new Date(a.t)).slice(0,5);$('#recent').className=recentItems.length?'rows':'empty';$('#recent').innerHTML=recentItems.length?recentItems.map(x=>`<div class="row"><span>${fmt(x.t)}</span><strong>${safe(x.label)}</strong></div>`).join(''):'No activity yet.';const summary=$('#summaryState');if(usable<8){summary.className='summaryState';summary.innerHTML=`<strong>Not enough information yet</strong><p>${usable} usable recording${usable===1?'':'s'} available. MOM chooses not to over-interpret a small personal history.</p>`}else{summary.className='summaryState good';summary.innerHTML='<strong>Personal history ready for evaluation</strong><p>Enough usable recordings exist to evaluate whether repeatable personal relationships are present. This does not guarantee a meaningful result.</p>'}}
function renderSessions(){if(!profile){$('#sessionsList').className='empty';$('#sessionsList').textContent='Choose a profile to view sessions.';return}if(!sessions.length){$('#sessionsList').className='empty';$('#sessionsList').innerHTML='No recordings yet. Start a guided recording when the device is connected.';return}$('#sessionsList').className='';$('#sessionsList').innerHTML=sessions.map(s=>`<article class="sessionCard"><div><strong>${fmt(s.started_at)}</strong><div class="sessionMeta"><span>${s.duration_seconds||'—'} sec</span><span>${safe(s.quality_label||'quality pending')}</span><span>${s.learning_eligible?'Used for learning':'Saved for reference'}</span></div></div><button class="btn ghost" data-session="${s.id}">View</button></article>`).join('');$$('[data-session]').forEach(b=>b.onclick=()=>openSession(b.dataset.session))}
$('#refreshSessions').onclick=loadData;
function openSession(id){const s=sessions.find(x=>x.id===id);if(!s)return;const c=checkins.find(x=>x.session_id===id);$('#sessionDetail').innerHTML=`<div class="qualityRows"><div><span>Date</span><strong>${fmt(s.started_at)}</strong></div><div><span>Duration</span><strong>${s.duration_seconds||'—'} sec</strong></div><div><span>Recording quality</span><strong>${safe(s.quality_label||'Pending')}</strong></div><div><span>Learning status</span><strong>${s.learning_eligible?'Used for learning':'Saved for reference'}</strong></div><div><span>Check-in</span><strong>${c?.hunger_rating!=null?Number(c.hunger_rating).toFixed(1)+'/10':'None'}</strong></div></div><p class="small">This session contributes only to this profile's history. One recording alone does not determine an experimental summary.</p><div class="cta"><button class="btn" id="exportSession">Export session data</button><button class="btn danger" id="deleteSession">Delete session</button></div>`;$('#sessionModal').classList.remove('hidden');$('#exportSession').onclick=()=>downloadJson(`mom-session-${id}.json`,{session:s,checkin:c||null});$('#deleteSession').onclick=async()=>{if(!confirm('Delete this session? This cannot be undone.'))return;const r=await sb.from('mom_sessions').delete().eq('id',id);if(r.error)return alert(r.error.message);$('#sessionModal').classList.add('hidden');await loadData()}}
$('#closeSession').onclick=()=>$('#sessionModal').classList.add('hidden');$('#sessionModal').onclick=e=>{if(e.target.id==='sessionModal')$('#sessionModal').classList.add('hidden')};
function renderInsights(){const total=sessions.length, usable=usableCount(), checks=checkins.length;$('#totalSessions').textContent=total;$('#usableSessions').textContent=usable;$('#checkinCount').textContent=checks;let label='No history yet',text='Start with a guided recording to begin building this profile’s research history.';if(total>0){label=usable<4?'Early history':usable<8?'Growing history':'Established personal dataset';text=`This profile has ${total} recording${total===1?'':'s'}, ${usable} usable, and ${checks} optional check-in${checks===1?'':'s'}.`;}$('#evidenceLabel').textContent=label;$('#evidenceText').textContent=text}
function setRecordStep(n){recordStep=n;$$('.recordStep').forEach(x=>x.classList.toggle('hidden',Number(x.dataset.step)!==n));$$('.stepper span').forEach((x,i)=>x.classList.toggle('on',i===n));if(n===1)renderRecordReadiness();window.scrollTo({top:0,behavior:'smooth'})}
$$('[data-next-record]').forEach(b=>b.onclick=()=>setRecordStep(Math.min(4,recordStep+1)));$$('[data-prev-record]').forEach(b=>b.onclick=()=>setRecordStep(Math.max(0,recordStep-1)));$('#placementBtn').onclick=()=>alert('Use the same selected abdominal position for repeated recordings. Hold the stethoscope-style sensor comfortably and keep contact steady during the one-minute capture.');
function renderRecordReadiness(){const online=isOnline(),ok=!!profile&&online;$('#beginRecording').disabled=!ok;if(!profile){$('#preQualityTitle').textContent='Choose a profile first';$('#preQualityText').textContent='A profile keeps the recording attached to the correct personal history.';$('#recordDisabledWhy').textContent='Start becomes available after a profile is selected and a paired device is online.'}else if(!online){$('#preQualityTitle').textContent='Device not connected';$('#preQualityText').textContent='The dashboard is online, but the physical device has not checked in recently.';$('#recordDisabledWhy').textContent='Start becomes available when a paired device has checked in recently.'}else{$('#preQualityTitle').textContent='Device ready';$('#preQualityText').textContent='A paired device checked in recently. Keep the sensor steady before starting.';$('#preQuality').querySelector('.dot').classList.add('good');$('#qualityPreview').innerHTML='<div><span>Contact consistency</span><strong>Live check begins with recording</strong></div><div><span>Background noise</span><strong>Live check begins with recording</strong></div><div><span>Motion stability</span><strong>Live check begins with recording</strong></div><div><span>Clipping / gain</span><strong>Live check begins with recording</strong></div>';$('#recordDis